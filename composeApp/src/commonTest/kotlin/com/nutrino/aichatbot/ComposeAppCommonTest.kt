package com.nutrino.aichatbot

import kotlin.test.Test
import kotlin.test.assertEquals

/**
 * Sample common test suite for the shared module.
 */
class ComposeAppCommonTest {

    /**
     * Verifies that the test framework is wired correctly with a simple arithmetic assertion.
     */
    @Test
    fun example() {
        assertEquals(3, 1 + 2)
    }
}